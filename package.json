{
  "name": "regression-eval-repo-2",
  "version": "1.0.0",
  "private": true,
  "description": "A simple Express health check server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
