{
  "name": "healthz-server",
  "version": "1.0.0",
  "private": true,
  "description": "Simple Express server with a health check endpoint",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
