{
  "name": "healthz-server",
  "version": "1.0.0",
  "private": true,
  "description": "A minimal Express health-check server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
