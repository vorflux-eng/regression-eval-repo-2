{
  "name": "healthz-express-server",
  "version": "1.0.0",
  "private": true,
  "description": "A simple Express server with a health check endpoint.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
