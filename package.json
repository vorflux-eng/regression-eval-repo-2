{
  "name": "regression-eval-repo-2",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Express server exposing a GET /healthz endpoint.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test tests/healthz.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
