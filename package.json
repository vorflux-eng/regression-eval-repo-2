{
  "name": "piyushhhxyz-server",
  "version": "1.0.0",
  "description": "Simple Express server with health check endpoint",
  "license": "MIT",
  "main": "src/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --verbose",
    "pretest": "npm ci --include=dev --ignore-scripts"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
