{
  "name": "healthz-server",
  "version": "1.0.0",
  "description": "Small Express server with a /healthz endpoint",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
