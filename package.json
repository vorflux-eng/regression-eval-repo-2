{
  "name": "regression-eval-repo-2",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Express server with a health check endpoint",
  "main": "src/server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
